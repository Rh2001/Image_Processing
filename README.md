# Image_Processing
My submission for the Intelligent Systems course at LUT University.

The uploaded Python file trains a simple 4 layer CNN(Convolutional Neural Network) and fine-tunes the existing HuggingFace ResNet50 model by unfreezing two layers, applying a new classification head and trying different hyperparameters.
